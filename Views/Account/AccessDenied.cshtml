@{
    ViewData["Title"] = "Access Denied";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-5 text-center">
        <div class="card p-5">
            <h2 class="mt-3 text-danger">Access Denied</h2>
            <p class="text-muted mt-2">
                This page requires the <strong>Admin</strong> role. You are logged in as a regular user.
            </p>
            <a asp-controller="Dashboard" asp-action="Index" class="btn btn-primary mt-3">Back to Dashboard</a>
        </div>
    </div>
</div>